// This will only show when the html file is viewed in a browser using Live Server and viewing the console in the Inspector

// select all unordered list (ul) elements
const allUnorderedLists = document.querySelectorAll("ul");
console.log(allUnorderedLists);
// select all elements with the class "class-week"
const allClassWeeks = document.getElementsByClassName("class-week");
console.log(allClassWeeks);
// select all elements with the class "nav-bar"
const allClassNav = document.getElementsByClassName("nav-bar");
console.log(allClassNav);
// select the element with the id "dog-picture" and save it to a variable
let pictName = document.getElementById("dog-picture");
console.log(pictName);
